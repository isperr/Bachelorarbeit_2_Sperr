import styled from 'styled-components';

const Logo = styled.img`
  width: 10rem;
  border-radius: 50%;
  box-shadow: 0px 3px 20px 2px rgba(51,51,51,0.75);
  margin-top: 1.5em;
`;

export default Logo;
